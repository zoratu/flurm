# SLURM Client Configuration for Migration Testing
# Used by FLURM and test-runner to communicate with SLURM

ClusterName=migration-test
SlurmctldHost=slurm-controller
SlurmctldPort=6817

# Authentication
AuthType=auth/munge

# Required minimal settings for client tools
MpiDefault=none
ProctrackType=proctrack/linuxproc
ReturnToService=2
SlurmctldPidFile=/var/run/slurm/slurmctld.pid
SlurmdPidFile=/var/run/slurm/slurmd.pid
SlurmdSpoolDir=/var/spool/slurm/d
SlurmUser=slurm
StateSaveLocation=/var/spool/slurm/ctld
SwitchType=switch/none
TaskPlugin=task/none

# Scheduling
SchedulerType=sched/backfill
SelectType=select/cons_tres
SelectTypeParameters=CR_Core

# Logging
SlurmdDebug=info
SlurmctldDebug=info

# Node definitions (must match controller)
NodeName=compute-node-1 CPUs=4 RealMemory=4000 State=UNKNOWN

# Partition definitions (must match controller)
PartitionName=debug Nodes=compute-node-1 Default=YES MaxTime=00:30:00 State=UP
PartitionName=batch Nodes=compute-node-1 MaxTime=24:00:00 State=UP
PartitionName=gpu Nodes=compute-node-1 MaxTime=48:00:00 State=UP
