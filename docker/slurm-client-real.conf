# SLURM Configuration for Client Testing Against Real SLURM
# Points to real SLURM controller

ClusterName=slurm-test
SlurmctldHost=slurm-controller
SlurmctldPort=6817
SlurmdPort=6818

# Authentication
AuthType=auth/munge

# Process tracking (required for client configuration)
ProctrackType=proctrack/linuxproc

# Scheduler (required for client configuration)
SchedulerType=sched/backfill
SelectType=select/cons_tres
SelectTypeParameters=CR_Core

# Misc
MpiDefault=none
SwitchType=switch/none
TaskPlugin=task/none

# PIDs
SlurmctldPidFile=/var/run/slurmctld.pid
SlurmdPidFile=/var/run/slurmd.pid

# Spool directories (required)
StateSaveLocation=/var/spool/slurmctld
SlurmdSpoolDir=/var/spool/slurmd

# User
SlurmUser=root

# Node Definition - must match real SLURM cluster
NodeName=slurm-node-001 CPUs=4 RealMemory=4096 State=UNKNOWN

# Partition Definition
PartitionName=batch Nodes=slurm-node-001 Default=YES MaxTime=INFINITE State=UP
